<h1> BUY ME! </h1>

<h2> <% @items.each do |item| %>
   <a href="/items/<%= item.id%>" > <%= item.name %> </a>
      <%= item.price%>
     <%=  item.description %>
     <br> 
     <br>
   <% end%>
  </h2>
<button class="btn btn-primary"> Bootstrap Button</button>