<h2> New Image for <%= @item.name %> </h2>

<%= form_tag "/items/#{@item.id}/images", method: :post do %>
  <div>
    <%= label_tag :url %>
    <%= text_field_tag :url %>
  </div>
  <%= submit_tag "Submit" %>
<% end %>  